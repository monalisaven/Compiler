# hello #
program fibonacci
    declare k,z,y;
	function fib (in x)
    {
		return (fibonacci(in x-1)*fibonacci(in x-2));
	}
    # main #
	{
		input(x);
		print(fibonacci(in x));
    }
.
